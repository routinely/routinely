= f.simple_fields_for :payload do |p|
  = p.input :recipient, input_html: { value: f.object.payload&.[]("recipient") || current_user.phone }
  = p.input :message, as: :text, input_html: { value: f.object.payload&.[]("message") }
