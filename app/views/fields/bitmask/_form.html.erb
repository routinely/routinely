<div class="field-unit__label">
  <%= f.label field.attribute %>
</div>
<div class="field-unit__field">
  <%= f.collection_check_boxes field.attribute, f.object.class.send("values_for_#{field.attribute}"), :itself, :itself do |b| %>
    <%= b.label + b.check_box %>
  <% end %>
</div>
